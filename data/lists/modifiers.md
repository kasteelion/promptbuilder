# Outfit Modifiers

Modifiers are now defined locally within each outfit file in `data/outfits/`. 

This global file is kept for backward compatibility and as a fallback, but all primary modifiers have been migrated to their respective outfit files.

---

## Legacy / Global Fallback
(None currently active)